.DS_Store
.idea/
node_modules/

# Brand source kit — kept local; GitHub Pages serves everything committed
Full Stack Lendding Brand Identity Kit/

# Jekyll (local build only; GitHub Pages builds its own copy on push)
_site/
.jekyll-cache/
.jekyll-metadata
vendor/
.bundle/

# Internal dev/assistant notes — kept local, never published to the public repo
CLAUDE.md
